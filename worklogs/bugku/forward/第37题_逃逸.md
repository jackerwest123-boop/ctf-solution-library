# 第37题：逃逸

## 基本信息
- 平台：Bugku CTF
- 题库URL：https://ctf.bugku.com/challenges/index/gid/2/tid/1/status/-1.html
- 全局题号：第37题
- 题目名称：逃逸
- 题目类型：WEB
- 所属赛事：NUAACTF-2020
- 题目链接：https://ctf.bugku.com/challenges/detail/id/547.html
- 是否有附件：否
- 附件类型：无

## 题面
nuaactf{}（列表确认题名，详情页当前缓存抓取失败）

## 分析过程
题名“逃逸”通常指模板逃逸、沙箱逃逸、过滤逃逸或 PHP 字符串/序列化逃逸。当前只能确认该题位于 Bugku 比赛真题 WEB 第2页，属于 NUAACTF-2020 WEB 题；详情页 id/547 当前无法抓取，无法确认具体语言、框架和源码。

## 解题步骤
1. 启动场景后抓取首页源码、响应头和静态资源。
2. 判断逃逸类型：SSTI 模板逃逸、PHP 反序列化字符串逃逸、过滤器逃逸或沙箱对象逃逸。
3. 若为模板逃逸，枚举模板引擎指纹并读取对象链。
4. 若为字符串逃逸，分析过滤前后字符串长度变化，构造属性逃逸 payload。
5. 触发读取 flag 文件或环境变量。

## 使用工具和命令
```bash
curl -i '<scene_url>'
burp
tplmap -u '<scene_url>?name=test'
```

## 结果
flag：未取得最终 flag（需动态场景复现）。

## 可复用性判断
- 是否可复用：是
- 可复用技法：按“模板/沙箱/字符串/过滤器逃逸”四类路径定位逃逸点并构造 payload。
- 是否需要写 solver：否，需先确认具体逃逸类型。
- 是否产生 artifact：否

## blocked 情况
- blocked：是
- 卡住原因：详情页缓存抓取失败，当前环境无法启动/访问 Bugku 动态场景。
- 已尝试方法：点击列表中第37题链接，确认指向 detail id/547。
- 下一步建议：启动场景后确认具体源码和逃逸类型，再补全 payload 与 flag。
